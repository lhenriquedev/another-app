import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
  },
  content: {},
  header: {
    marginTop: 16,
    paddingHorizontal: 16,
    flexDirection: 'row',
    gap: 8,
    marginBottom: 16,
  },
});
